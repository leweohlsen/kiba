export type Group = {
    id: string;
    name: string;
}

export type Person = {
    id: string;
    name: string;
    groupId: string;
}

export type Product = {
    id: string;
    name: string;
    price: number;
}